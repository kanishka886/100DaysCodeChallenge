#include <stdio.h>
int main(){

int b, l;
 printf("what is the lenght and breadth of your rectangle?");
 scanf ("%d %d", &l, &b );

 printf("The area of the rectangle is %d and the parameter is %d", l*b, 2*(l+b));

	
	return 0;
}